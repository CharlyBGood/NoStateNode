<div class="container">
  <div class="">
    <div class="">
      <div class="">
        <h1>Update</h1>
        <form action="/tasks/{{task._id}}/edit" method="POST">

          <div class="mb-3">
            <input
              type="text"
              name="title"
              placeholder="Title"
              value="{{task.title}}"
              class=""
            />
          </div>

          <div class="">
            <input
              name="description"
              type="url"
              placeholder="share link"
              value="{{task.description}}"
              id="description"
              class="form-control rounded-0 bg-dark"
            />
            {{!-- <textarea
              name="description"
              rows="3"
              placeholder="Add a description"
              class="form-control rounded-0 bg-dark text-white"
            >
              {{task.description}}
            </textarea> --}}
          </div>

          <button class="btn btn-dark">Update</button>
        </form>
      </div>
    </div>
  </div>
</div>